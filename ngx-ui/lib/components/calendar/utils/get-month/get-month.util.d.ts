import moment from 'moment';
/**
 * Returns the month offset correctly
 *
 * @param active
 * @return days
 */
export declare function getMonth(active: moment.Moment): import("../../calendar-month.type").CalendarMonth;
